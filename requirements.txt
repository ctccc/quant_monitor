# 数据抓取
requests>=2.31

# 低频数据渠道(交易日历/新闻/龙虎榜等)
akshare>=1.14

# Web 服务与调度(M0 起)
fastapi>=0.100
uvicorn[standard]>=0.23
apscheduler>=3.10,<4
pyyaml>=6.0

# akshare 自带 pandas 依赖,此处不重复声明。
# M3(盘中实时)阶段再引入: httpx(异步并发)、pyarrow(快照落 Parquet)。
